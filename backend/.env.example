# Copia este archivo como .env y ajusta los valores
PORT=3000
JWT_SECRET=cambia-esta-clave-en-produccion
JWT_EXPIRES_IN=8h
# Ruta del archivo SQLite (relativa a /backend)
DB_FILE=crm.db
# Origen permitido para CORS (el front). Usa * solo en desarrollo.
CORS_ORIGIN=*
